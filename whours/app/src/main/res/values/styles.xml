<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="ThemeOverlay.AppCompat.ActionBar">
        <!-- Customize your theme here. -->
    </style>
    <style name="AppTheme_ActionBar" parent="@android:style/Theme.Holo.Light.DarkActionBar">
    </style>

    <!-- Base application theme. -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.CompactMenu">
        <!-- Customize your theme here. -->
    </style>

</resources>
